//
//  main.swift
//  swiftTypeCasting
//
//  Created by Vedant Mistry on 18/09/21.
//

import Foundation

/*//type casting in swift programming

func getClientPet() -> Animal{
    //returns the pet
}
let pet = getClientPet() //pet is the type of animal
//functions for the action to be performed
func walk(dog: Dog){ //for dog animal
    print("Walking \(dog.name)")
}
func cleanLitterBox(cat: Cat){ //for cat animal
    print("Cleaning the cat's \(cat.boxSize) litter box.")
}
func cleanCage(bird: Bird){ //for bird animal
    print("removing the \(bird.featherColor) feathers of the bird.")
}
let pets = allClientAnimals() //pets refers to all the animals mentioned above
for pet in pets{ //for example: cat in dog, cat, bird
    if let dog = pet as? Dog{ //if dog is the specified animal
        walk(dog: Dog)
    }
    else if let cat = pet as? Cat{ //if cat is the specified animal
        cleanLitterBox(cat: Cat)
    }
    else if let bird = pet as? Bird{ //if bird is the specified animal
        cleanCage(bird: Bird)
    }
}
//us the 'as!' operator when the return type is specific*/

